{
  "name": "tfstate-drift-inspector-api",
  "version": "0.1.0",
  "description": "Cloudflare Workers API for Terraform drift detection",
  "main": "src/worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "jest tests/",
    "db:create": "wrangler d1 create drift_inspector",
    "db:migrate": "wrangler d1 execute drift_inspector --file=../d1/schema.sql"
  },
  "dependencies": {
    "itty-router": "^5.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240601.0",
    "jest": "^29.7.0",
    "wrangler": "^4.105.0"
  }
}